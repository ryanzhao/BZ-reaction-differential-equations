# BZ 反应微分方程模拟（C++，可弹窗动画）

现在仓库包含：

- `docs/bz_model_and_simulation.md`：从 ODE 到 PDE、以及螺旋波数学描述与论文索引。
- `src/main.cpp`：Oregonator 风格 2D 模拟器，支持：
  - **实时窗口动画（X11）**
  - **可选保存帧为 PPM**（用于合成视频）
  - **培养皿风格渲染 + 螺旋外扩波纹**

## 1) 编译

```bash
g++ -std=c++17 -O2 -o bz_sim src/main.cpp -lX11
```

## 2) 运行（弹窗动画）

```bash
./bz_sim
```

## 3) 无界面生成视频

```bash
./bz_sim --save --headless --steps=22000 --wave-mode=spiral --view=mix --pacemaker-period=240 --layers=4 --ring-spacing=12
ffmpeg -framerate 30 -i output/frame_%06d.ppm -pix_fmt yuv420p bz.mp4
```

## 4) 推荐命令（接近实验中的螺旋演化）

### A. 螺旋向外扩散（默认推荐）

```bash
./bz_sim --view=mix --wave-mode=spiral --pacemaker-period=240 --layers=4 --ring-spacing=12 --save --headless --steps=24000
```

### B. 靶心波（中心反复外扩）

```bash
./bz_sim --view=mix --wave-mode=outward --pacemaker-period=160 --save --headless --steps=16000
```

### C. 外向内 + 中心向外同时出现（双向演化）

```bash
./bz_sim --view=mix --wave-mode=dual --pacemaker-period=260 --save --headless --steps=18000
```

## 5) 常用参数

- `--steps=50000` 设置步数
- `--dt=0.0022` 设置时间步长
- `--nx=320 --ny=320` 设置网格
- `--scale=2` 设置窗口像素放大倍数
- `--view=u|w|mix` 选择显示字段（默认 `mix`）
- `--wave-mode=outward|inward|dual|spiral` 选择波纹模式
- `--pacemaker-period=260` 设置周期激发间隔（越小波纹出现越频繁）
- `--no-pacemaker` 关闭周期激发源
- `--contrast=auto|fixed` 自动对比度增强（默认 `auto`，波纹更清楚）
- `--layers=4` 每次起搏同时生成多层环形波前
- `--ring-spacing=12` 多层环之间的间距

## 6) WSL 更新后编译报 `redeclaration` 的快速修复

如果你 `git pull` 之后，`g++` 提示大量 `redeclaration of Params::...`，说明 `src/main.cpp` 在你的本地被错误拼接/冲突残留。

按下面顺序在 WSL 执行：

```bash
cd ~/BZ-reaction-differential-equations

# 1) 备份你当前文件（防止丢失）
cp src/main.cpp src/main.cpp.bak.$(date +%Y%m%d_%H%M%S)

# 2) 丢弃本地 main.cpp，恢复为远端 main 的干净版本
git fetch origin
git checkout origin/main -- src/main.cpp

# 3) 编译
g++ -std=c++17 -O2 -o bz_sim src/main.cpp -lX11

# 4) 重新生成视频
rm -rf output bz.mp4
./bz_sim --headless --save --steps=24000 --wave-mode=spiral --view=mix --pacemaker-period=240 --layers=4 --ring-spacing=12
ffmpeg -framerate 30 -i output/frame_%06d.ppm -pix_fmt yuv420p bz.mp4
```

如果你想本地分支和远端完全一致（会丢弃未提交改动）：

```bash
git fetch origin
git reset --hard origin/main
git clean -fd
```


## 7) 运行时报 `./bz_sim: No such file or directory` 怎么办

这个错误表示**可执行文件还没生成**（或你不在项目目录）。

按下面顺序执行：

```bash
cd ~/BZ-reaction-differential-equations
pwd
ls -la src/main.cpp

# 先编译，生成 bz_sim
g++ -std=c++17 -O2 -o bz_sim src/main.cpp -lX11

# 确认文件存在
ls -la bz_sim

# 再运行生成帧
rm -rf output bz.mp4
./bz_sim --headless --save --steps=24000 --wave-mode=spiral --view=mix --pacemaker-period=240 --layers=4 --ring-spacing=12

# 最后合成视频
ffmpeg -framerate 30 -i output/frame_%06d.ppm -pix_fmt yuv420p bz.mp4
```

如果编译阶段报错，就先不要执行 `./bz_sim` 和 `ffmpeg`，把编译报错贴出来再处理。
