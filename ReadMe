# BZ 反应微分方程模拟（C++，可弹窗动画）

现在仓库包含：

- `docs/bz_model_and_simulation.md`：从 ODE 到 PDE、以及螺旋波数学描述与论文索引。
- `src/main.cpp`：Oregonator 风格 2D 模拟器，支持：
  - **实时窗口动画（X11）**
  - **可选保存帧为 PPM**（用于合成视频）
  - **培养皿风格渲染 + 螺旋外扩波纹**
- `docs/bz_model_and_simulation.md`：从 ODE 到 PDE（反应-扩散）的数学建模说明。
- `src/main.cpp`：Oregonator 风格 2D 模拟器，支持：
  - **实时窗口动画（X11）**
  - **可选保存帧为 PPM**（用于合成视频）

## 1) 编译

```bash
g++ -std=c++17 -O2 -o bz_sim src/main.cpp -lX11
```

## 2) 运行（弹窗动画）

```bash
./bz_sim
```

## 3) 无界面生成视频

```bash
./bz_sim --save --headless --steps=22000 --wave-mode=spiral --view=mix --pacemaker-period=240 --layers=4 --ring-spacing=12
./bz_sim --save --headless --steps=18000 --wave-mode=spiral --view=mix
ffmpeg -framerate 30 -i output/frame_%06d.ppm -pix_fmt yuv420p bz.mp4
```

## 4) 推荐命令（接近实验中的螺旋演化）

### A. 螺旋向外扩散（默认推荐）

```bash
./bz_sim --view=mix --wave-mode=spiral --pacemaker-period=240 --layers=4 --ring-spacing=12 --save --headless --steps=24000
./bz_sim --view=mix --wave-mode=spiral --pacemaker-period=140 --save --headless --steps=20000
```

### B. 靶心波（中心反复外扩）

```bash
./bz_sim --view=mix --wave-mode=outward --pacemaker-period=160 --save --headless --steps=16000
```

### C. 外向内 + 中心向外同时出现（双向演化）

```bash
./bz_sim --view=mix --wave-mode=dual --pacemaker-period=260 --save --headless --steps=18000
./bz_sim --view=mix --wave-mode=dual --pacemaker-period=150 --save --headless --steps=18000
./bz_sim --save --headless --steps=16000
ffmpeg -framerate 30 -i output/frame_%06d.ppm -pix_fmt yuv420p bz.mp4
```

## 4) 更接近实验波纹的推荐命令

### A. 中心向外反复扩散（target waves）

```bash
./bz_sim --view=mix --wave-mode=outward --pacemaker-period=160 --save --headless --steps=16000
```

### B. 外向内 + 中心向外同时出现（双向演化）

```bash
./bz_sim --view=mix --wave-mode=dual --pacemaker-period=150 --save --headless --steps=18000
```

### C. 只看慢变量（颜色更像氧化还原）

```bash
./bz_sim --view=w --wave-mode=outward --pacemaker-period=180 --save --headless --steps=16000
```

## 5) 常用参数

- `--steps=50000` 设置步数
- `--dt=0.0022` 设置时间步长
- `--dt=0.0032` 设置时间步长
- `--nx=320 --ny=320` 设置网格
- `--scale=2` 设置窗口像素放大倍数
- `--view=u|w|mix` 选择显示字段（默认 `mix`）
- `--wave-mode=outward|inward|dual|spiral` 选择波纹模式
- `--pacemaker-period=260` 设置周期激发间隔（越小波纹出现越频繁）
- `--no-pacemaker` 关闭周期激发源
- `--contrast=auto|fixed` 自动对比度增强（默认 `auto`，波纹更清楚）
- `--layers=4` 每次起搏同时生成多层环形波前
- `--ring-spacing=12` 多层环之间的间距
- `--pacemaker-period=150` 设置周期激发间隔（越小波纹出现越频繁）
- `--no-pacemaker` 关闭周期激发源
- `--contrast=auto|fixed` 自动对比度增强（默认 `auto`，波纹更清楚）
- `--dt=0.0035` 设置时间步长
- `--nx=260 --ny=260` 设置网格
- `--scale=3` 设置窗口像素放大倍数
- `--view=u|w|mix` 选择显示字段（默认 `mix`）
- `--wave-mode=outward|inward|dual` 选择激发方向模式
- `--pacemaker-period=180` 设置周期激发间隔（越小波纹出现越频繁）
- `--no-pacemaker` 关闭周期激发源
- `--contrast=auto|fixed` 自动对比度增强（默认 `auto`，波纹更清楚）
# BZ Reaction Differential Equations

This repository now includes:

- `BZ_simulation_design.md`: mathematical design notes for Oregonator ODE/PDE modeling.
- `src/bz_simulation.cpp`: a starter C++ reaction–diffusion simulator that writes PPM frames.

## Build

```bash
g++ -std=c++17 -O2 -o bz_sim src/bz_simulation.cpp
```

## Run

```bash
./bz_sim
```

说明：
- 会弹出窗口，显示 BZ 波纹颜色变化（红=还原态，蓝=氧化态）。
- 按任意键或关闭窗口可提前结束。

## 3) 无界面运行（例如服务器）

```bash
./bz_sim --headless
```

## 4) 保存帧并合成视频

```bash
./bz_sim --save --headless
ffmpeg -framerate 30 -i output/frame_%06d.ppm -pix_fmt yuv420p bz.mp4
```

## 5) 如果你看到“中心蓝点不动”

可以用下面这个命令，开启默认 pacemaker 的同时，把可视化改成 `u+w` 混合前沿（更容易看到波在传播）：

```bash
./bz_sim --view=mix --save --headless --steps=12000
```

如果你想只看慢变量（颜色更像“红氧化态”），可以改成：

```bash
./bz_sim --view=w
```

## 常用参数

- `--steps=50000` 设置步数
- `--dt=0.004` 设置时间步长
- `--nx=240 --ny=240` 设置网格
- `--scale=3` 设置窗口像素放大倍数
- `--view=u|w|mix` 选择显示字段（默认 `mix`）
- `--no-pacemaker` 关闭周期激发源
- `--pacemaker-period=320` 设置周期激发间隔
The simulation writes `frame_XXXXX.ppm` images. Convert to video:

```bash
ffmpeg -framerate 20 -i frame_%05d.ppm -pix_fmt yuv420p bz.mp4
```
